---
"cezartdev-skills": patch
---

Enforce strict Zero-Comments code policy across all workflow subagent prompts and execution directives (no `//`, `#`, or `""" """` in generated code unless explicitly requested).
