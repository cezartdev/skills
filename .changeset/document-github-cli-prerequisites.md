---
"cezartdev-skills": patch
---

Document GitHub CLI (`gh`) prerequisite and token scopes for issue reading, pull request curation, and repository management, and add `gh` diagnostic reporting to `workflow check-env`.
