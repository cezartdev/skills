---
"cezartdev-skills": patch
---

Automatically remove `.gitkeep` files as soon as real files or specifications are created inside `.workflow/` subdirectories, and reconcile placeholders if directories become empty.
