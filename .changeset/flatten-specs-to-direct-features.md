---
"cezartdev-skills": minor
---

Flatten `.workflow/specs/` directory layout by eliminating `bugs/`, `docs/`, `refactor/`, and `features/` namespaces; specifications now live directly in `.workflow/specs/<spec>/` as agnostic feature containers with co-located ADRs.
