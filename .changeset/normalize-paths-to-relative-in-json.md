---
"cezartdev-skills": patch
---

Normalize `worktree_path` in `daemons.json` and `spec_path` in `state.json` to clean project-relative paths (`.workflow/...`) to preserve user privacy and portability across machines.
