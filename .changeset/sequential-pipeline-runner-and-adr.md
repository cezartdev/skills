---
"cezartdev-skills": minor
---

Implement deterministic 4-stage sequential subagent pipeline runner (`workflow run <spec>`), automated MADR Architectural Decision Record generation, and streamlined positional CLI commands (`status`, `stop`, `clean`, `curate`).
