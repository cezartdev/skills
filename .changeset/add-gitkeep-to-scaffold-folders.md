---
"cezartdev-skills": patch
---

Automatically generate `.gitkeep` placeholder files inside all base `.workflow/` scaffold directories (`specs/`, `memory/`, `prs/`, `issues/`) so Git preserves empty folder structures.
