---
"cezartdev-skills": minor
---

Implement worker branch scoping (`<spec>-<worker>`), spec-based auto-merge targeting, and Curator subagent unification into `<spec>-curator-worker` with PR suggestions to `<spec>`.
